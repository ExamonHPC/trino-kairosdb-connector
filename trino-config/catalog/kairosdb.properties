connector.name=kairosdb
kairosdb.url=http://kairosdb:8080

# Optional: pick how the synthetic "timestamp" column is exposed to SQL.
# Accepted values: BIGINT (default, epoch ms), TIMESTAMP_MILLIS, TIMESTAMP_TZ.
#kairosdb.timestamp.format=TIMESTAMP_TZ
